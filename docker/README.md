# Docker Foundation

Local services:

- PostgreSQL 16
- Redis 7
- MinIO S3-compatible object storage
- NestJS API
- Next.js web app

Copy `.env.example` to `.env` before starting the stack. Database migrations should be run explicitly rather than hidden inside container startup.
