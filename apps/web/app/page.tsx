export default function HomePage() {
  return (
    <main className="shell">
      <section className="panel">
        <p className="eyebrow">Foundation Ready</p>
        <h1>Broadcast Operations ERP</h1>
        <p>
          The monorepo, API, database schema, desktop shell, and deployment foundation are ready
          for staged module implementation.
        </p>
      </section>
    </main>
  );
}
